package com.ustglobal.demo;

import javax.crypto.NullCipher;

class Sup{
	Number add() {
		return 0;
	}
}
public class TestA extends Sup{
	@Override             //overriding we can change non primitive into its sub class if then return type is non primitive if it premitive we cannot change
	Integer add() {  
		return 4;
	}
	Arrays
}